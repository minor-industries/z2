{
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "tsc && cp `find lib -type f` dist && node esbuild.config.mjs"
  },
  "dependencies": {
    "calendar": "github:minor-industries/calendar#v0.0.2",
    "rtgraph": "github:minor-industries/rtgraph#js/module"
  }
}
