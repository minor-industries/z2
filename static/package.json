{
  "devDependencies": {
    "esbuild": "^0.23.0",
    "esbuild-plugin-copy": "^2.1.1",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build-wasm": "(cd .. && GOOS=js GOARCH=wasm go build -o static/dist/z2.wasm .)",
    "build-js": "tsc",
    "build-dist": "node esbuild.config.mjs",
    "build": "npm run build-wasm && npm run build-js && npm run build-dist"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "calendar": "github:minor-industries/calendar#v0.0.2",
    "rtgraph": "file:../../rtgraph",
    "uuid": "^10.0.0"
  }
}
